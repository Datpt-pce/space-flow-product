{
  "schemaVersion": 2,
  "packageId": "pipeline-broken-contract-test",
  "version": "1.0.0",
  "displayName": "Pipeline Broken Contract Test",
  "category": "data",
  "description": "Test fixture only — proves the Phase 6 registry submission pipeline (backend/registry/pipeline/) rejects a submission whose tests/contract.json doesn't match its actual output (step 06), landing in ChangesRequested, not a production node.",
  "icon": "package",
  "runtime": { "type": "javascript", "entry": "execute.js" },
  "inputs": [],
  "outputs": [
    { "id": "items", "label": "Items", "type": "array" }
  ],
  "config": [],
  "capabilities": {},
  "limits": { "timeoutSeconds": 10, "memoryMB": 128, "maxOutputMB": 16 },
  "compatibility": { "spaceFlow": ">=1.0.0" }
}
