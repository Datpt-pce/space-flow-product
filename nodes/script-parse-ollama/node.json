{
  "id": "script-parse-ollama",
  "name": "Script Parse (Ollama, local)",
  "version": "1.1.0",
  "runsOn": "local",
  "category": "ai",
  "description": "Stage 2 — lựa chọn local/fallback (specs/english-teaching-video-pipeline.md §4.1, §0), tương tự nodes/script-parse-llm/ nhưng gọi Ollama chạy local (http://localhost:11434) thay vì DeepSeek cloud — không cần credential/internet. Cùng system prompt/output shape với script-parse-llm để 2 node hoán đổi được cho nhau trên canvas, kể cả dot System Prompt/Reference mở rộng.",
  "icon": "file-json",
  "inputs": [
    { "id": "user_prompt", "label": "User Prompt (Script Text)", "type": "text", "required": true },
    { "id": "system_prompt", "label": "System Prompt", "type": "text", "required": false },
    { "id": "reference", "label": "Reference", "type": "text", "required": false }
  ],
  "outputs": [
    { "id": "scenes", "label": "Scenes (Items)", "type": "array" },
    { "id": "json", "label": "Raw JSON", "type": "text" },
    { "id": "error", "label": "Error", "type": "json" }
  ],
  "config": [
    { "id": "ollamaUrl", "label": "Ollama Base URL (để trống để dùng Local Services registry trong Settings, hoặc điền URL cụ thể để override riêng cho node này)", "type": "text", "default": "" },
    { "id": "model", "label": "Model", "type": "text", "default": "qwen2.5:7b" },
    { "id": "lessonTitle", "label": "Lesson Title (optional, đưa vào prompt)", "type": "text", "default": "" },
    { "id": "temperature", "label": "Temperature", "type": "number", "default": 0.3, "min": 0, "max": 2, "step": 0.1 }
  ],
  "executor": "execute.js"
}
